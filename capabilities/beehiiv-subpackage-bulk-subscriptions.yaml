naftiko: 1.0.0-alpha2
info:
  label: API Reference — subpackage_bulk_subscriptions
  description: 'API Reference — subpackage_bulk_subscriptions. 1 operations. Lead operation: Bulk create subscription <Badge
    intent="info" minimal outlined>OAuth Scope: subscriptions:write</Badge>. Self-contained Naftiko capability covering one
    Beehiiv business surface.'
  tags:
  - Beehiiv
  - subpackage_bulk_subscriptions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    BEEHIIV_API_KEY: BEEHIIV_API_KEY
capability:
  consumes:
  - type: http
    namespace: beehiiv-subpackage-bulk-subscriptions
    baseUri: https://api.beehiiv.com/v2
    description: API Reference — subpackage_bulk_subscriptions business capability. Self-contained, no shared references.
    resources:
    - name: publications-publicationId-bulk_subscriptions
      path: /publications/{publicationId}/bulk_subscriptions
      operations:
      - name: create
        method: POST
        description: 'Bulk create subscription <Badge intent="info" minimal outlined>OAuth Scope: subscriptions:write</Badge>'
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicationId
          in: path
          type: string
          description: The prefixed ID of the publication object
          required: true
        - name: Authorization
          in: header
          type: string
          description: Bearer authentication
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.BEEHIIV_API_KEY}}'
  exposes:
  - type: rest
    namespace: beehiiv-subpackage-bulk-subscriptions-rest
    port: 8080
    description: REST adapter for API Reference — subpackage_bulk_subscriptions. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/publications/{publicationid}/bulk-subscriptions
      name: publications-publicationid-bulk-subscriptions
      description: REST surface for publications-publicationId-bulk_subscriptions.
      operations:
      - method: POST
        name: create
        description: 'Bulk create subscription <Badge intent="info" minimal outlined>OAuth Scope: subscriptions:write</Badge>'
        call: beehiiv-subpackage-bulk-subscriptions.create
        with:
          publicationId: rest.publicationId
          Authorization: rest.Authorization
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: beehiiv-subpackage-bulk-subscriptions-mcp
    port: 9090
    transport: http
    description: MCP adapter for API Reference — subpackage_bulk_subscriptions. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: bulk-create-subscription-badge-intent-info
      description: 'Bulk create subscription <Badge intent="info" minimal outlined>OAuth Scope: subscriptions:write</Badge>'
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: beehiiv-subpackage-bulk-subscriptions.create
      with:
        publicationId: tools.publicationId
        Authorization: tools.Authorization
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
